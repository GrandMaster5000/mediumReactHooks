import React from 'react'
import ArticleForm from 'components/articleForm';


const CreateArticle = () => {
    const errors = {};
    const initValues = {};
    const handleSubmit = data => {
        console.log(data);
    }
    return (
        <div>
            <ArticleForm errors={errors} initValues={initValues} onSubmit={handleSubmit}/>
        </div>
    )
}

export default CreateArticle;